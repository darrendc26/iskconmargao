FROM golang:1.23-alpine AS build
WORKDIR /src
RUN apk add --no-cache git ca-certificates
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -ldflags="-s -w" -o /out/backup ./cmd/backup

FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata postgresql16-client openssl
WORKDIR /app
COPY --from=build /out/backup /app/backup
ENV TZ=Asia/Kolkata
ENTRYPOINT ["/app/backup"]
